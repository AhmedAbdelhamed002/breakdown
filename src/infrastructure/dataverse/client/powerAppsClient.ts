// Wrapper around the Power Apps Code Apps SDK initialization.
// In production, initialize @microsoft/power-apps here and expose the client.
export async function initializePowerApps(): Promise<void> {
  // await initialize(); // from @microsoft/power-apps
  return Promise.resolve();
}
